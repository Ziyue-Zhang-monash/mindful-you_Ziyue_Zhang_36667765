{
  "name": "mindful-you",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx firebase-tools deploy --only hosting",
    "cloudflare:dev": "npm run build && npx wrangler pages dev dist"
  },
  "dependencies": {
    "firebase": "^12.17.1",
    "vue": "^3.5.38"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "vite": "^8.0.16"
  }
}
